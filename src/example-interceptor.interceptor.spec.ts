import { ExampleInterceptorInterceptor } from './example-interceptor.interceptor';

describe('ExampleInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ExampleInterceptorInterceptor()).toBeDefined();
  });
});
